{
  "name": "trip-link-functions",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf lib",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions:moderateCommentDeepSeek"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^7.2.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.6",
    "rimraf": "^6.0.1",
    "typescript": "^5.8.3"
  }
}
